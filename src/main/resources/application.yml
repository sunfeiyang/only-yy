spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/yy?characterEncoding=utf-8
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

AsyncConfig:
  corePoolSize: 10
  maxPoolSize: 200
  queueCapacity: 10